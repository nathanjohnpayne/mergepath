# Blocked Codex request evidence (#1276)

The normal external merge-clearance delegate opts into informational request evidence on approval/external-clearance blocks. The existing BLOCKED reason, approval and clearance predicates, exit statuses, carry-forward, query modes, readiness and successful paths remain unchanged. No acknowledgement reads occur outside this opted-in block path.

A trigger qualifies by the existing configured-author and freshness predicate; it has no immutable SHA attribution. Select its latest qualifying comment, report its source timestamp and age, and check the configured bot's eyes reaction on that exact comment at or after its creation. Report configured acknowledgement/review budgets without claiming an active waiter, remaining retry count or a new deadline. Unreadable acknowledgement evidence and unavailable/invalid timing or budgets are unknown, never absent.

Use the existing current-head review/verdict/summary observations and account-block evidence. Provider activity without a qualifying author trigger is still activity; do not claim nothing is in flight. A terminal artifact without required approval retains the unmet-clearance diagnosis. Missing qualifying request and no matching provider activity can advise requesting through the existing helper. This adds no trigger, retry, new signal grammar, reaction carry-forward, workflow or persistent state. Coverage: `tests/test_codex_request_evidence.sh` plus existing request/ack and merge-clearance suites.
